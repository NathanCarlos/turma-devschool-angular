import { BlueDirective } from './blue.directive';

describe('BlueDirective', () => {
  it('should create an instance', () => {
    const directive = new BlueDirective();
    expect(directive).toBeTruthy();
  });
});
