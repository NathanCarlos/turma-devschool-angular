import { ChangeTextPipe } from './change-text.pipe';

describe('ChangeTextPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeTextPipe();
    expect(pipe).toBeTruthy();
  });
});
